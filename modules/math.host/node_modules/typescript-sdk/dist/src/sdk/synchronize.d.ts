import { ICart, IResponseAuth, IShopifyCart } from "../common/interfaces";
import { SdkService } from "../common/services/sdk";
export declare class CartSynchronize {
    private sdkService;
    private utils;
    shopifyCart: IShopifyCart;
    cart: ICart;
    authData: IResponseAuth;
    loaderModal: any;
    constructor(shopifyCart: IShopifyCart, sdkService: SdkService);
    updateCart(cart: ICart): void;
    private setContentModal();
    synchronize(): void;
    private detectChangesStoreItemsAndSkyItems(itemsSky, itemsStore, currentCountry?);
    private deleteProductsFromCart(restricteditems, itemsStore, currentCountry?);
    private modalProductsRestricted(html);
    private editShopifyItemsData(items);
    private addProduct(productos?);
    private editProduct(editedProducts);
    private removeProduct(productId, promise?);
    private removeCart();
    private reloadCart();
    private setHtmlConceptDetail(conceptHtml);
    validateRestriction(arraySky: any[], arrayStore: any[], currentCountry: string): {
        objUpdate: any;
        html: string;
    };
}
