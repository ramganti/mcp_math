import { Observable } from "rxjs/Observable";
export declare class Storage {
    private expiration_name;
    private subscribers;
    constructor();
    private initStorageWithExpiration();
    private setExpirationToKey(k);
    get(key: string): Object;
    set(key: string, value: Object, expiration?: number, callback?: Function): void;
    remove(key: string): void;
    getExpirationIntervalFromKey(key: string): Observable<number>;
}
